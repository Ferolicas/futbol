# Supabase Auth (login, sesiones, password reset).
# Las tablas de datos están en el VPS Postgres (ver DATABASE_URL abajo).
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# VPS PostgreSQL (todas las tablas de datos: user_profiles, match_*, tickets, …)
# Formato: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://cfanalisis:PASSWORD@87.106.236.248:5432/cfanalisis
# `false` desactiva SSL (no recomendado en producción). Vacío = SSL relaxed.
DATABASE_SSL=
DATABASE_POOL_MAX=10

# Sanity CMS (legacy — kept for editorial content only)
NEXT_PUBLIC_SANITY_PROJECT_ID=
SANITY_API_TOKEN=
SANITY_DATASET=production

# Football API (api-sports.io)
FOOTBALL_API_KEY=
# BZZOIRO_API_KEY  ← DEPRECATED, no leido por ningun codigo. Borrar de Vercel.
# BASEBALL_API_KEY ← DEPRECATED, purgado al migrar a MLB Stats API (gratuita,
#                    sin key). Borrar de Vercel/VPS si todavia existe.

# Baseball: la fuente de datos es MLB Stats API (statsapi.mlb.com, gratuita,
# sin API key) + The Odds API para cuotas (compartida con futbol).

# NextAuth (DEPRECATED — auth real lo hace Supabase Auth via @supabase/ssr;
# en Fase 2.5 migramos a custom PG VPS auth y borramos estas vars).
# NEXTAUTH_SECRET=
# NEXTAUTH_URL=

# URL publica de la app — usada por API routes para construir URLs absolutas
# (Stripe redirect, webhook callbacks, email links).
NEXT_PUBLIC_APP_URL=https://cfanalisis.com

# Stripe (get from https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_COUPON_50_ID=

# Resend (emails transaccionales: bienvenida, reset contraseña, plan activado)
# El dominio del FROM_EMAIL debe estar verificado en Resend (SPF + DKIM).
RESEND_API_KEY=
FROM_EMAIL=CF Analisis <info@cfanalisis.com>
ADMIN_EMAIL=ferneyolicas@gmail.com

# ────────────────────────────────────────────────────────────────────────────
# Pusher (DEPRECATED) — el realtime migro a WebSocket nativo via el worker.
# Estas vars siguen aqui solo como referencia historica; los hooks ahora usan
# NEXT_PUBLIC_WORKER_WS_URL + NEXT_PUBLIC_WORKER_SECRET (ver abajo).
# PUSHER_APP_ID=
# PUSHER_KEY=
# PUSHER_SECRET=
# PUSHER_CLUSTER=us2
# NEXT_PUBLIC_PUSHER_KEY=
# NEXT_PUBLIC_PUSHER_CLUSTER=us2
# ────────────────────────────────────────────────────────────────────────────

# Worker en VPS — endpoint /enqueue/* y /broadcast (server-side), y /ws (cliente).
# WORKER_URL: usado por API routes de Next.js para enqueue jobs y emitir eventos.
# En VPS (mismo host), apunta a localhost:8080 para evitar round-trip via Caddy.
# En Vercel (legacy), era https://worker.cfanalisis.com.
WORKER_URL=http://localhost:8080
# WORKER_SECRET: secreto SERVER-ONLY. Protege /admin, /enqueue, /broadcast del
# worker. NUNCA debe ir a una var NEXT_PUBLIC_ (no debe llegar al navegador).
WORKER_SECRET=
# WS_PUBLIC_TOKEN (C1 FIX): token SOLO para el WebSocket de clientes (read-only).
# DEBE ser un valor ALEATORIO DISTINTO de WORKER_SECRET. Su gemelo cliente es
# NEXT_PUBLIC_WS_TOKEN (mismo valor). Genera: openssl rand -base64 32
WS_PUBLIC_TOKEN=
# NEXT_PUBLIC_*: usado por el navegador para conectarse al WebSocket realtime.
NEXT_PUBLIC_WORKER_WS_URL=wss://worker.cfanalisis.com/ws
# NEXT_PUBLIC_WS_TOKEN = mismo valor que WS_PUBLIC_TOKEN (NO el WORKER_SECRET).
# Es lo único que viaja al cliente; al ser distinto del admin secret, aunque se
# extraiga del bundle NO da acceso a /admin /enqueue /broadcast.
NEXT_PUBLIC_WS_TOKEN=
# DEPRECADO (C1): ya NO uses NEXT_PUBLIC_WORKER_SECRET (filtraba el admin secret
# al cliente). Déjalo vacío; el cliente usa NEXT_PUBLIC_WS_TOKEN.
NEXT_PUBLIC_WORKER_SECRET=

# ────────────────────────────────────────────────────────────────────────────
# Redis — VPS local (Upstash eliminado en Fase 4 — 2026-05-23).
# lib/redis.js usa ioredis TCP directo. Mismo Redis local que BullMQ + n8n.
# ────────────────────────────────────────────────────────────────────────────
LOCAL_REDIS_HOST=127.0.0.1
LOCAL_REDIS_PORT=6379

# ────────────────────────────────────────────────────────────────────────────
# Error tracking — el worker usa Pino + alertas Telegram con dedup 1/min.
# Las vars TELEGRAM_BOT_TOKEN y TELEGRAM_ALERT_CHAT_ID viven en el .env del
# worker en el VPS, NO aqui. El frontend (Vercel) loguea con console.error,
# Vercel los archiva en su dashboard de logs.
# ────────────────────────────────────────────────────────────────────────────

# The Odds API (https://the-odds-api.com)
THE_ODDS_API_KEY=

# Cron Protection
CRON_SECRET=your-cron-secret-here

# Web Push Notifications (generate with: node -e "const wp=require('web-push'); const k=wp.generateVAPIDKeys(); console.log(k)")
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
