// Inherits dashboard/layout.js (auth + SportToggle + chat).
export const metadata = {
  title: 'Baseball - CFanalisis',
};

export default function BaseballLayout({ children }) {
  return <>{children}</>;
}
